Hello<% if (user_name = searls_auth_helper.attr_for(@user, @config.user_name_method)).present? %>, <%= user_name %><% end %>!

Reset your password using this link:
<%= password_reset_edit_url({ token: @token, redirect_path: @redirect_path, redirect_subdomain: @redirect_subdomain, redirect_host: @redirect_host }.compact_blank) %>

If you didn't ask for this, you can ignore this email.
